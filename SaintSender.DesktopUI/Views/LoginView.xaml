<Window x:Class="SaintSender.DesktopUI.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SaintSender.DesktopUI.Views"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <DockPanel>
        <Grid DockPanel.Dock="Bottom">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <Button Grid.Column="0" MinHeight="50" Click="Btn_Sign_in">Sign In</Button>
            <Button Grid.Column="1" MinHeight="50" IsCancel="True">Cancel</Button>
        </Grid>
        <StackPanel Margin="50,0" MinHeight="35">
            <Label Margin="10">Username</Label>
            <TextBox Margin="0,10,10,10" x:Name="txtUsername" MinHeight="35"></TextBox>
            <Label Margin="10">Password</Label>
            <TextBox  Margin="0,10,10,10" x:Name="txtPassword" MinHeight="35"></TextBox>
        </StackPanel>
    </DockPanel>
</Window>
